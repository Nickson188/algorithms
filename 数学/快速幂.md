```cpp
typedef long long LL;

int qpow(int a, int b, int p)
{
    int s = 1;
    a %= p;
    while (b)
    {
        if (b & 1) s = (LL)s * a % p;
        a = (LL)a * a % p;
        b >>= 1;
    }
    return s;
}
```
